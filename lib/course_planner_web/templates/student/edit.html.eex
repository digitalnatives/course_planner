
  <%= render "form.html", title: CoursePlannerWeb.SharedView.display_user_name(@changeset.data),
                          changeset: @changeset,
                          action: student_path(@conn, :update, @student),
                          conn: @conn
  %>
